# Rust's output directory
target

# Local settings
.soroban
.stellar

# Generated test auth snapshots
test_snapshots/

# Node / frontend
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# packages/*/dist is committed on purpose: Vercel builds only the frontend
# workspace, so the generated contract-client bindings need to already be
# built rather than relying on a separate prebuild step.

# Editor/OS cruft
.Rhistory
.DS_Store
